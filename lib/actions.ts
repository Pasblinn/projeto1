"use server"

// Este arquivo serve apenas como ponte para as ações do cliente
// Todas as operações reais são feitas no cliente usando localStorage

export async function createScan(formData: FormData) {
  // Esta função é apenas um placeholder para compatibilidade com o código existente
  // A implementação real está no cliente usando localStorage
  return { success: true }
}

export async function getScan(id: string) {
  // Esta função é apenas um placeholder para compatibilidade com o código existente
  // A implementação real está no cliente usando localStorage
  return null
}

export async function getAllScans() {
  // Esta função é apenas um placeholder para compatibilidade com o código existente
  // A implementação real está no cliente usando localStorage
  return []
}

export async function updateScan(id: string, data: any) {
  // Esta função é apenas um placeholder para compatibilidade com o código existente
  // A implementação real está no cliente usando localStorage
  return null
}

export async function deleteScan(id: string) {
  // Esta função é apenas um placeholder para compatibilidade com o código existente
  // A implementação real está no cliente usando localStorage
  return true
}
